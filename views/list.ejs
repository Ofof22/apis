<!-- views/list.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Awesome API List</title>
  
  <!-- Yeni stil ekleniyor -->
  <style>
    /* Genel stil */
    body {
      font-family: 'Arial', sans-serif;
      background-color: #1a1a1a;
      margin: 0;
      padding: 0;
      color: #fff;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }

    header {
      background-color: #333;
      color: #fff;
      padding: 20px;
      text-align: center;
    }

    h1 {
      margin: 0;
      font-size: 36px;
      letter-spacing: 2px;
    }

    main {
      background-color: #222;
      padding: 20px;
    }

    h2 {
      color: #ffcc00;
      font-size: 24px;
    }

    p {
      color: #fff;
    }

    a {
      color: #ffcc00;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    a:hover {
      color: #fff;
    }

    .overlay {
      display: none;
      position: fixed;
      z-index: 1;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.5);
    }

    .modal {
      background-color: #333;
      color: #fff;
      border-radius: 10px;
      padding: 20px;
      width: 50%;
      position: absolute;
      left: 25%;
      top: 25%;
    }

    .modal-text {
      margin-bottom: 20px;
    }

    .modal-button {
      background-color: #ffcc00;
      color: #000;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      cursor: pointer;
    }

    .awesome-button {
      background-color: #ffcc00;
      color: #000;
      border: none;
      padding: 15px 30px;
      border-radius: 10px;
      font-size: 18px;
      font-weight: bold;
      cursor: pointer;
    }

    .awesome-button:hover {
      background-color: #ff9900;
    }
  </style>
  <!-- Yeni stil eklemesi sonu -->
</head>

<body>
  <div class="container">
    <main>
      <ul>
        <% data.forEach(item => { %>
          <li>
            <h2><%= item.title %></h2>
            <p><%= item.description %></p>
            <button onclick="showConfirmation('https://uptrical.onrender.com/api/<%= item.url %>')" class="awesome-button">API ye erişim</button>
          </li>
        <% }); %>
      </ul>
    </main>
  </div>

  <!-- Onay kutusu -->
  <div id="confirmationOverlay" class="overlay">
    <div class="modal">
      <p>Yönlendirme sırasında sorumluluk bizde değildir. Yapılacak olan her şey sizin üzerinizedir. API'yi kötü amaçlı kullanım size kötü sonuçlar döndürebilir.</p>
      <p>Yönlendirilmek istiyor musunuz?</p>
      <button onclick="redirectToApi()" class="modal-button">Evet</button>
      <button onclick="closeConfirmation()" class="modal-button">Hayır</button>
    </div>
  </div>

  <script>
    var currentUrl; // Global değişken

    function showConfirmation(url) {
      currentUrl = url;
      document.getElementById('confirmationOverlay').style.display = 'block';
    }

    function closeConfirmation() {
      document.getElementById('confirmationOverlay').style.display = 'none';
    }

    function redirectToApi() {
      window.location.href = currentUrl;
    }
  </script>
</body>

</html>
